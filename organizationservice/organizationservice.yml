example.organization.property: "I AM THE DEFAULT ORGANIZATION SERVICE"

example.property: "I AM THE DEFAULT VERSION"
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true
        dialect: "org.hibernate.dialect.MySQL5Dialect"
  datasource:
    platform: "mysql"
    url: "jdbc:mysql://localhost:3306/db_example?useSSL=false"
    username: root
    password: secret

redis:
  server: "redis"
  port: "6379"

signing:
  key: "345345fsdfsf5345"